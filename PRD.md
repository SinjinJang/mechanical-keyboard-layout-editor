# PRD: Mechanical Keyboard Layout Editor v2

## 1. 제품 개요

**제품명**: Mechanical Keyboard Layout Editor v2
**버전**: 0.1.0 (Beta)
**데모**: https://sinjinjang.github.io/mechanical-keyboard-layout-editor/

기계식 키보드 DIY 제작자를 위한 웹 기반 키보드 레이아웃 편집기. 사용자가 키 스위치를 시각적으로 배치하고, 속성을 편집하며, 3D/2D 모델 파일(STL/DXF)을 생성할 수 있다.

## 2. 문제 정의

- 기계식 키보드를 직접 설계하려면 CAD 도구에 대한 전문 지식이 필요하다.
- 인체공학적 키보드 레이아웃을 시각적으로 실험하고 반복 수정하기 어렵다.
- 설계한 레이아웃을 3D 프린팅/CNC 밀링용 파일로 변환하는 과정이 복잡하다.

## 3. 대상 사용자

| 사용자 유형 | 설명 |
|---|---|
| DIY 키보드 제작자 | 자신만의 레이아웃을 설계하고 실물로 제작하려는 사용자 |
| 키보드 동호회 | 커스텀 레이아웃을 공유하고 피드백을 주고받는 커뮤니티 |
| 프로토타이핑 | 인체공학적 배열을 빠르게 실험하려는 설계자 |

## 4. 현재 구현 완료 기능

### 4.1 시각적 레이아웃 편집
- 드래그 앤 드롭으로 키 스위치 배치 (0.25U 그리드 스냅)
- 클릭으로 개별 키 선택/해제
- 실시간 시각적 피드백

### 4.2 키 속성 편집
- 라벨 편집
- 너비 선택 (1U ~ 7U, 다양한 규격)
- 높이 선택 (1U, 2U)
- 회전 각도 조절 (-90° ~ +90°)
- 키 추가/삭제

### 4.3 파일 관리
- JSON 형식으로 레이아웃 다운로드/업로드
- 서버에서 사전 정의된 레이아웃 불러오기
- 자동 플레이트 크기 계산

### 4.4 3D/2D 모델 생성
- STL 파일 생성 (3D 프린팅용)
- DXF 파일 생성 (CNC 밀링용)
- 이메일 기반 전송 (AWS Lambda 백엔드)

### 4.5 레이아웃 정보 표시
- 실시간 키 개수
- 동적 플레이트 크기 (U 단위)

## 5. 기술 스택

| 영역 | 기술 |
|---|---|
| 프레임워크 | React 18, React Router 5 |
| 상태 관리 | @hookstate/core 3.x |
| UI | Material-UI v4, Bootstrap 5 |
| 드래그 앤 드롭 | react-draggable |
| 빌드 | Create React App (react-scripts 5) |
| 배포 | GitHub Pages (gh-pages) |
| 백엔드 | AWS Lambda (ap-northeast-2) |
| 테스트 | Jest, React Testing Library |

## 6. 미구현 기능 및 로드맵

### Phase 1: 안정성 강화
| 항목 | 우선순위 | 설명 |
|---|---|---|
| Error Boundary 추가 | 높음 | 컴포넌트 에러 격리 및 사용자 안내 |
| JSON 스키마 검증 강화 | 높음 | 업로드 파일의 유효성 검증 (ajv 활용) |
| 테스트 커버리지 확대 | 중간 | 주요 컴포넌트 단위/통합 테스트 |

### Phase 2: 편집 기능 고도화
| 항목 | 우선순위 | 설명 |
|---|---|---|
| 다중 키 선택 및 그룹 편집 | 높음 | Shift/Ctrl 클릭으로 복수 키 선택 |
| Undo/Redo | 높음 | 편집 이력 관리 (Ctrl+Z/Y) |
| 키 복사/붙여넣기 | 중간 | 선택한 키를 복제 |
| 회전 키 충돌 감지 | 중간 | 회전된 키 간의 겹침 방지 |
| 키보드 단축키 | 낮음 | Delete, 방향키 이동 등 |

### Phase 3: 사용자 경험 개선
| 항목 | 우선순위 | 설명 |
|---|---|---|
| 사전 정의 레이아웃 카테고리 필터링 | 중간 | TKL, 60%, 75% 등 카테고리별 분류 |
| 레이아웃 미리보기 | 중간 | 불러오기 전 시각적 미리보기 |
| 접근성 개선 | 중간 | ARIA 레이블, 키보드 내비게이션 |
| 반응형 디자인 | 낮음 | 모바일/태블릿 대응 |

### Phase 4: 모델 생성 개선
| 항목 | 우선순위 | 설명 |
|---|---|---|
| OpenJSCAD 클라이언트 렌더링 | 높음 | 서버 의존 없이 브라우저에서 3D 모델 생성 |
| 모델 생성 진행률 표시 | 중간 | 서버 처리 상태 실시간 표시 |
| 3D 미리보기 | 낮음 | 브라우저 내 3D 모델 프리뷰 |

### Phase 5: 코드 품질
| 항목 | 우선순위 | 설명 |
|---|---|---|
| TypeScript 마이그레이션 | 중간 | 타입 안전성 확보 |
| 성능 최적화 | 낮음 | React.memo, useMemo 적용 |
| MUI v4 → v5 마이그레이션 | 낮음 | 최신 Material-UI 버전 적용 |

## 7. 데이터 모델

```typescript
interface KeySwitch {
  label: string;  // 키 라벨 텍스트
  x: number;      // X 좌표 (U 단위)
  y: number;      // Y 좌표 (U 단위)
  w: number;      // 너비 (U 단위)
  h: number;      // 높이 (U 단위)
  a: number;      // 회전 각도 (degrees)
}

type Layout = KeySwitch[];
```

## 8. 아키텍처

```
App
├── Header
├── HashRouter
│   ├── Home
│   │   └── KeyPlate (메인 편집 캔버스)
│   │       ├── LayoutMenu (파일/모델 관리)
│   │       ├── EditPanel (속성 편집기)
│   │       └── KeySwitch[] (개별 키 - 드래그 가능)
│   └── About
└── Footer
```

- **단위 체계**: 1U = 60px, 그리드 스냅 = 0.25U (15px), 패딩 = 15px
- **상태 관리**: Hookstate로 레이아웃 배열과 선택 인덱스 관리
- **백엔드 API**: `https://pk92p8sd0k.execute-api.ap-northeast-2.amazonaws.com/main`

## 9. 제약 사항 및 리스크

| 항목 | 설명 |
|---|---|
| 서버 리소스 제한 | AWS Lambda 프리티어 사용으로 모델 생성 속도 제한 (~30분) |
| 단일 큐 처리 | 모델 생성 요청이 순차 처리됨 |
| MUI v4 지원 종료 | Material-UI v4는 더 이상 적극 유지보수되지 않음 |
| CRA 지원 종료 | Create React App은 더 이상 권장되지 않음 (Vite 등 대안 고려) |

## 10. 성공 지표

- 레이아웃 설계 완료까지의 사용자 작업 시간 단축
- 모델 생성 성공률
- 사전 정의 레이아웃 활용률
- 파일 업로드/다운로드 오류율 감소
